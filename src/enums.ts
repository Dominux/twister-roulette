export enum Side {
  Right = 'Right',
  Left = 'Left',
}

export enum Limb {
  Hand = 'Hand',
  Foot = 'Foot',
}

export enum Color {
  Red = 'Red',
  Yellow = 'Yellow',
  Green = 'Green',
  Blue = 'Blue',
}
